// Re-export from site-context so all existing imports keep working unchanged.
// The real implementation is in lib/site-context.tsx (root-level Provider).
export { useSiteSettings } from './site-context'
